// Add metadata for the Dine-in Policy page
export const metadata = {
  title: 'Cafe Policies',
  description: 'Review the dine-in policies for GLENROAST cafe.',
};

import DineInPolicyPageClient from '@/components/DineInPolicyPageClient';

export default function DineInPolicyPage() {
  return <DineInPolicyPageClient />;
} 